const navButtons=document.querySelectorAll("[data-page]");const pages=document.querySelectorAll(".page");const sidebar=document.querySelector(".sidebar");const menuBtn=document.getElementById("menuBtn");function showPage(pageId){pages.forEach(page=>page.classList.toggle("active",page.id===pageId));navButtons.forEach(btn=>btn.classList.toggle("active",btn.dataset.page===pageId));if(window.innerWidth<1000)sidebar.classList.remove("open")}navButtons.forEach(btn=>{btn.addEventListener("click",()=>{const target=document.getElementById(btn.dataset.page)?btn.dataset.page:"ela";showPage(target)})});menuBtn?.addEventListener("click",()=>sidebar.classList.toggle("open"));if("serviceWorker"in navigator){window.addEventListener("load",()=>{navigator.serviceWorker.register("./service-worker.js").catch(console.error)})}